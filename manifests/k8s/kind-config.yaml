kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  # add a mount from /path/to/my/files on the host to /files on the virtual node
  extraMounts:
  - hostPath: /home/kooper/Projects/k8s/ssh/ssh-ingress/tmp/volume
    containerPath: /volume